<div class="flex flex-col items-center justify-center min-h-screen px-4">
  <div class="max-w-2xl w-full text-center space-y-8">
    <div class="space-y-4">
      <h1 class="text-6xl font-bold tracking-tight">
        <span class="text-red-500">Rails</span>Bot
      </h1>
      <p class="text-xl text-gray-400">Talk to Ruby on Rails itself.</p>
      <p class="text-sm text-gray-500">Powered by convention over configuration since 2004.</p>
    </div>

    <%= link_to "New Conversation", new_conversation_path,
        class: "inline-block bg-red-600 hover:bg-red-700 text-white font-semibold py-3 px-8 rounded-lg text-lg transition-colors" %>

    <% if @conversations.any? %>
      <div class="mt-12 space-y-2">
        <h2 class="text-sm font-medium text-gray-500 uppercase tracking-wider mb-4">Recent Conversations</h2>
        <%= render @conversations %>

        <% if @has_more %>
          <turbo-frame id="conversations_page_<%= @next_page %>">
            <%= link_to "Load more", conversations_path(page: @next_page),
                class: "block text-center py-3 text-sm text-gray-400 hover:text-gray-200 transition-colors",
                data: { turbo_frame: "conversations_page_#{@next_page}" } %>
          </turbo-frame>
        <% end %>
      </div>
    <% end %>
  </div>

  <footer class="mt-16 mb-8 text-center space-y-1">
    <p class="text-sm text-gray-500">Made with <span class="text-red-500">&hearts;</span> and <code class="text-gray-400">bundler</code></p>
    <p class="text-xs text-gray-600">v<%= RailsBot::Version::STRING %> &middot; Updated <%= time_ago_in_words(RailsBot::Version::LAST_UPDATED) %> ago</p>
    <p class="text-xs text-gray-700">Latest Fix: <%= RailsBot::Version::CHANGELOG %></p>
  </footer>
</div>
